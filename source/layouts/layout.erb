<% wrap_layout :master_layout do %>

    <section class="title-container clearfix" style="background: url(<%= current_page.data.immagineTestata %>) center no-repeat; background-size: cover;">
        <div class="float-center">
            <div class="row">
                <div class="small-12 columns title">
                    <span class="big">
                        <%= current_page.data.testata.prima_riga || "Defibrillatore Philips" %><br class="show-for-large-up">
                    </span>
                    <span class="medium">
                        <%= current_page.data.testata.seconda_riga || "Il più venduto al mondo" %><br />
                    </span>

                    <% if current_page.data.testata.hasButton != 0  %>
                        <span>
                            <%= partial "partials/callToAction" %>
                        </span>
                    <% end %>
                </div>
            </div>

        </div>
    </section>

    <%= yield %>


<% end %>